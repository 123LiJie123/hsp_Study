- 请看下面的问题
    1、根据配置文件 re.properties 指定信息，创建对象并调用方法
        classfullpath = com.hspedu.Cat
        method = hi
        :使用现有技术，能做吗？

    2、 这样的需求在学习框架时特别多，即通过外部文件配置，在不修改源码情况下，来控制程序，也符合设计模式ocp原则（开闭原则）

    3、快速入门ReflectionQuestion.java